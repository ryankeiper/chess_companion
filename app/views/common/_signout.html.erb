<% if signed_in? %>
	<%= form_for :session, url: signout_path, html: {class: "sign-out"}, method: 'delete' do |f| %>
		Signed in as <%= current_user.name %>
		<%= f.submit 'Sign out', id: 'signout' %>
	<% end %>
<% end %>